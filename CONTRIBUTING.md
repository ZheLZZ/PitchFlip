# 反馈与贡献

欢迎反馈可复现的问题、交互改进或提交小范围修复。

## 报告问题

请说明 Windows 版本、软件版本、操作步骤、预期行为与实际结果。优先使用 `samples` 中的测试文件；只有必要时才附脱敏的最小 PDF。

不要上传客户材料、个人信息、保密 PDF、密码或访问令牌。截图请隐藏私人文件名和路径。

## 提交修改

- 将改动限制在一个明确问题，说明用户可见的行为变化。
- 原文件保护、奇偶页映射、原始页不可删除、VirtualBlank 不导出是核心约束。
- 编排或导出改动运行 `dotnet test -c Release`；预览交互改动运行 `npm run test:switch`。
- PDF 导出逻辑变化时，额外运行独立引擎验证，不能仅检查页数。
- 不提交 bin、obj、dist、node_modules、临时输出或私人 PDF。
- 引入依赖时说明用途，并补充对应第三方许可证。

详细环境说明见 [开发与测试](docs/DEVELOPMENT.md)。
