# MVP 验证记录

## 参考图界面更新（2026-09-10）

- `dotnet test -c Release`：15 项测试通过。
- `npm run test:switch`：PDF 切换、失败恢复、200 页定位、翻页、拖动、插空白目标、独占一张纸及页面总览回归通过。
- 新增检查通过：顶部打开／保存消息、右侧插入操作、原始页禁止删除、缩放后适合窗口恢复，以及 960×560 浏览区域的主要控件边界。
- 六页样本的左侧导航分组断言通过：封面、2–3 页、4–5 页、封底；2–3 页分别标注上页和下页。
- 已查看 1400×800 和 960×560 浏览区域截图；使用仓库合成 PDF，截图位于本地 artifacts/interface-redesign.png 和 artifacts/interface-compact.png。
- win-x64 自包含版本已重新构建至 dist/PitchFlip。此次浏览器测试使用真实 PDF.js，原生消息桥为测试替身；未重新手动验证系统文件对话框和实际打印机。

## 任务栏图标尺寸修复（2026-09-05）

原 Window.Icon 使用 BitmapImage，只解码 ICO 的首个 16px 帧，WPF 将其居中放入 64px 大图标，主体仅占约 16px。改用 ImageSource 类型转换器保留 BitmapFrame 的 ICO 多帧信息，按系统 DPI 选择对应尺寸。
`scripts/check_window_icon.ps1` 创建临时透明测试窗口，从 WM_GETICON 取得 Windows 实际使用的图标：修复前 64×64 图标的有效范围为 (24,25)-(40,40)，修复后为 (2,5)-(64,62)。32×32 小图标亦恢复正常占比。发布构建通过。

## 翻页与左侧列表同步（2026-09-05）

当前页变化时，左侧高亮缩略图自动进入可见区域，并给相邻插入按钮保留空间，不抢走键盘焦点。新增 ↓ / ↑ 翻页，页码输入框保持原有方向键行为。
浏览器回归覆盖：200 页文件跳至第 121 页后，把左栏滚回开头，再用底部按钮翻页；方向键前后翻页；拖动翻页；按 B 的插入目标与当前高亮页一致；输入框方向键不触发翻页。全部通过。

## PDF 切换修复（2026-09-05）

当前 PDF.js 6 的文档代理没有 destroy() 方法。改为保存并销毁 PDFDocumentLoadingTask，同时清理旧文档的缓存、缩略图任务和翻页动画。
真实浏览器回归测试先复现第二次打开时的 `pdf.destroy is not a function`，修复后连续切换 6/10/200 页、跳到旧文件页数之外、动画中切换、失败加载后重试均通过。测试入口：`npm run test:switch`。

2026-09-05，Windows 11 x64，.NET SDK 10.0.400。

- Release 构建与 win-x64 自包含发布成功。
- 9 个有实际断言的 xUnit 测试通过（模板空测试已移除）。
- 六页插入案例：输出 7 页，原第 5 页移到输出第 6 页，第 4 张背面为 VirtualBlank 且未导出。
- 原第 8 页插入案例：移动到输出第 9 页、第 5 张纸正面。
- 原始页删除被拒绝；插入、删除、撤销、重做及选择状态恢复通过。
- MediaBox、非默认 CropBox、旋转继承与副本导出通过。
- 拒绝覆盖源文件；失败保存保持未导出状态。
- 使用独立 PyMuPDF 引擎验证全部 6 个原页：文字和解压后的内容流完全一致；渲染差异限于透明度组带来的最多 2/255 颜色舍入，均值小于 0.1。
- 原生窗口实测：PDF.js 显示、异步缩略图、PageDown 顶部翻页、B 插空白、Ctrl+Z 撤销、Ctrl+Y 重做、鼠标上拖及封底状态。
- 原生保存对话框实测导出 samples/PitchFlip-6pages_Print.pdf，独立检查为 7 页，输出第 5 页为空白。
- 发布目录中的 PitchFlip.exe 已启动并显示 200 页 PDF（100 张纸）。

发现并修复：PDFsharp 缺省 CropBox getter 会写入零框；WebView2 虚拟映射不适用于动态 PDF 请求；普通 WebView2 消息的 AdditionalObjects 可能为 null。

未全面验证：真实 100 MB 文档、不同 DPI、实际打印机、所有特殊 PDF；拖入文件的宿主与前端代码已接入，尚未完成来自资源管理器的端到端拖入测试。范围限制见 README。
